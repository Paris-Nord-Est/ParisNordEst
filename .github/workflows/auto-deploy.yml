name: Auto Deploy (Tag + Purge CDN)

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'CHANGELOG.md'
      - 'docs/**'

jobs:
  auto-deploy:
    runs-on: ubuntu-latest
    name: Auto Tag and Purge CDN

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for all tags

      - name: Get latest tag
        id: get_latest_tag
        run: |
          # Get the latest tag, or default to v0.0.0 if no tags exist
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "Latest tag: $LATEST_TAG"

      - name: Analyze commits for version bump
        id: analyze_commits
        run: |
          LATEST_TAG="${{ steps.get_latest_tag.outputs.latest_tag }}"

          # Get commits since last tag
          if [ "$LATEST_TAG" = "v0.0.0" ]; then
            COMMITS=$(git log --pretty=format:"%s")
          else
            COMMITS=$(git log ${LATEST_TAG}..HEAD --pretty=format:"%s")
          fi

          echo "Commits since $LATEST_TAG:"
          echo "$COMMITS"
          echo ""

          # Determine version bump type based on conventional commits
          BUMP_TYPE="patch"

          # Check for breaking changes (major bump)
          if echo "$COMMITS" | grep -qiE "^(BREAKING CHANGE:|.*!:)"; then
            BUMP_TYPE="major"
            echo "Found BREAKING CHANGE - will bump major version"
          # Check for features (minor bump)
          elif echo "$COMMITS" | grep -qiE "^(feat|feature):"; then
            BUMP_TYPE="minor"
            echo "Found feat: commits - will bump minor version"
          # Check for fixes (patch bump)
          elif echo "$COMMITS" | grep -qiE "^(fix|bugfix|patch):"; then
            BUMP_TYPE="patch"
            echo "Found fix: commits - will bump patch version"
          else
            BUMP_TYPE="patch"
            echo "No conventional commits found - defaulting to patch version bump"
          fi

          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT

      - name: Calculate new version
        id: new_version
        run: |
          LATEST_TAG="${{ steps.get_latest_tag.outputs.latest_tag }}"
          BUMP_TYPE="${{ steps.analyze_commits.outputs.bump_type }}"

          # Remove 'v' prefix and split into major.minor.patch
          VERSION=${LATEST_TAG#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"

          # Bump version based on type
          case $BUMP_TYPE in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch)
              PATCH=$((PATCH + 1))
              ;;
          esac

          NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version will be: $NEW_VERSION (bump type: $BUMP_TYPE)"

      - name: Check if tag already exists
        id: check_tag
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"

          if git rev-parse "$NEW_VERSION" >/dev/null 2>&1; then
            echo "tag_exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ Tag $NEW_VERSION already exists, skipping deployment"
          else
            echo "tag_exists=false" >> $GITHUB_OUTPUT
            echo "✅ Tag $NEW_VERSION does not exist, will create and deploy it"
          fi

      - name: Create and push tag
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create annotated tag
          git tag -a "$NEW_VERSION" -m "Auto-release $NEW_VERSION"

          # Push tag to remote
          git push origin "$NEW_VERSION"

          echo "✅ Created and pushed tag: $NEW_VERSION"

      - name: Wait for jsDelivr to index new tag
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          echo "Waiting 30 seconds for jsDelivr to index the new tag..."
          sleep 30

      - name: Purge JavaScript bundle
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          echo "Purging app.js from jsDelivr cache..."
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/dist/app.js"

      - name: Purge CSS bundle
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          echo "Purging main.css from jsDelivr cache..."
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/dist/main.css"

      - name: Purge image assets
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          echo "Purging image assets from jsDelivr cache..."
          # Purge hero slider images
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/images/home/slider/542599450_17854130469531086_412195545774409256_n.webp"
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/images/home/slider/le-bar.jpg"
          # Purge banner image
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/images/home/banner/paris-nord-est-banner.jpg"
          # Purge logos
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/images/casquette/vectoriel/logo-horizontal-white.svg"
          curl -X POST "https://purge.jsdelivr.net/gh/Baldrani/ParisNordEst@latest/source/images/casquette/vectoriel/logo-round-white.svg"

      - name: Deployment summary
        if: steps.check_tag.outputs.tag_exists == 'false'
        run: |
          echo "## 🚀 Auto-Deploy Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Previous version:** \`${{ steps.get_latest_tag.outputs.latest_tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**New version:** \`${{ steps.new_version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Bump type:** \`${{ steps.analyze_commits.outputs.bump_type }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Completed:" >> $GITHUB_STEP_SUMMARY
          echo "1. Created tag \`${{ steps.new_version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "2. Purged jsDelivr cache (JS, CSS, images)" >> $GITHUB_STEP_SUMMARY
          echo "3. Changes are now live on your store!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📝 Commit Message Guide:" >> $GITHUB_STEP_SUMMARY
          echo "- \`feat:\` → minor version bump (v3.0.0 → v3.1.0)" >> $GITHUB_STEP_SUMMARY
          echo "- \`fix:\` → patch version bump (v3.0.0 → v3.0.1)" >> $GITHUB_STEP_SUMMARY
          echo "- \`BREAKING CHANGE:\` → major version bump (v3.0.0 → v4.0.0)" >> $GITHUB_STEP_SUMMARY

      - name: Skip summary
        if: steps.check_tag.outputs.tag_exists == 'true'
        run: |
          echo "## ⚠️ Tag Already Exists" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Tag \`${{ steps.new_version.outputs.new_version }}\` already exists." >> $GITHUB_STEP_SUMMARY
          echo "No deployment was performed." >> $GITHUB_STEP_SUMMARY
